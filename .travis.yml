language: ruby
cache:
- bundler
before_deploy: bundle exec middleman build --clean
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  local_dir: build
  on:
    branch: master
